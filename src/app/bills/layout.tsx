import type { Metadata } from "next";
export const metadata: Metadata = { title: "Legislation Tracker — CheckMyRep" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
