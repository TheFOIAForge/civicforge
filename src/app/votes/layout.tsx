import type { Metadata } from "next";
export const metadata: Metadata = { title: "Vote Lookup — CivicForge" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
