import type { Metadata } from "next";
export const metadata: Metadata = { title: "Key Issues — CheckMyRep" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
