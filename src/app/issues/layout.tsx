import type { Metadata } from "next";
export const metadata: Metadata = { title: "Key Issues — CitizenForge" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
