import type { Metadata } from "next";
export const metadata: Metadata = { title: "Members of Congress — CitizenForge" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
