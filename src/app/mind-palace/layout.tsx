import type { Metadata } from "next";
export const metadata: Metadata = { title: "Mind Palace AI — CitizenForge" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
