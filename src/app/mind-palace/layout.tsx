import type { Metadata } from "next";
export const metadata: Metadata = { title: "Mind Palace AI — CivicForge" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
